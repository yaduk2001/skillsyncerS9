const cron = require('node-cron');

/**
 * Initialize scheduled jobs
 */
function initializeScheduler() {
  console.log('🕐 Scheduler initialized (Mentor Allocation jobs disabled).');

  // Mentor allocation logic has been removed/deprecated.
  // Future scheduled tasks can be added here.
}

module.exports = {
  initializeScheduler
};
